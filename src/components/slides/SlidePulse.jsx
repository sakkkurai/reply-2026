import { PULSE_TITLE } from "../../constants/strings";
import { Text, Title } from "../ui/Typography";

export default function SlidePulse() {
    return (
        <div>
            <Title>{PULSE_TITLE}</Title>
        </div>
    );
}
